#include "main.h"
#include <stdio.h>
/**
* mul - function that multiply two integers
* Return: result
* @a: first argument
* @b: second argument
*/

int mul(int a, int b)
{
int result;

result = a * b;

return (result);
}
