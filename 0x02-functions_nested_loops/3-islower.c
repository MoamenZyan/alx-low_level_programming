#include "main.h"
/**
* _islower - this is the  function
* @c: takes input from other functions
* Return: 0 if upper and 1 if lower
*/
int _islower(int c)
{
	if (c > 90)

		return (1);
	else
		return (0);
}
